interface:
  display_name: "Draw.io Ultra"
  short_description: "用几何路由创建可编辑 Draw.io 图并导出文件"
  default_prompt: "Use $drawio-ultra to create this editable Draw.io diagram with calculated anchors, routed edges, and requested exports."
policy:
  allow_implicit_invocation: true
