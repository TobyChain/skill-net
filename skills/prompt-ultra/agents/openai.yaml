interface:
  display_name: "Prompt Ultra"
  short_description: "构建当前任务契约，并将稳定工作约定保存到正确的跨会话作用域"
  default_prompt: "Use $prompt-ultra to turn this request into an executable work contract and persist only the stable instructions at the correct scope."
policy:
  allow_implicit_invocation: true
