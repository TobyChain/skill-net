interface:
  display_name: "General"
  short_description: "Scope general tasks and persist standing instructions"
  default_prompt: "Use $general to scope this non-code task with a compact Work Contract, execute the authorized work, and persist, audit, or migrate standing instructions only when explicitly requested."
policy:
  allow_implicit_invocation: true
